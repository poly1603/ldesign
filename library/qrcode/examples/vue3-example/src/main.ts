/**
 * Vue 3 + TypeScript 示例应用入口文件
 * 展示 @ldesign/qrcode 在 Vue 3 环境中的使用
 */

import { createApp } from 'vue'
import App from './App.vue'
import './assets/main.css'

// 创建 Vue 应用实例
const app = createApp(App)

// 挂载应用
app.mount('#app')
