export { generateId, generateNumericId, generateShortId, generateTimestampId, generateUUID } from './id-generator.js';

/**
 * 工具函数
 */
declare function isValidInput(input: unknown): boolean;

export { isValidInput };
