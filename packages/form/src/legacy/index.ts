/**
 * 兼容层导出
 * 
 * @description
 * 导出兼容现有代码的组件和功能，确保平滑迁移
 */

// === 兼容组件 ===
// export { default as LDesignForm } from './form';
// export { default as LDesignFormItem } from './form-item';

// === 兼容 hooks ===
// export { useForm } from './hooks';

// === 兼容类型 ===
// export * from './type';

// === 兼容属性 ===
// export { default as props } from './props';

// === 默认导出 ===
export default {
  // LDesignForm,
  // LDesignFormItem,
  // useForm,
  // props
};

// 注意：这些导出将在实际实现时取消注释
// 兼容层的目的是确保现有的 _example 代码能够正常工作
