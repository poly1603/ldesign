# 色阶生成（Color Scales）

```ts
import {
  ColorScaleGenerator,
  generateColorScale,
  generateColorScales,
} from '@ldesign/color'
```

- generateColorScale(color, category, mode)
- generateColorScales(colors, mode)
- ColorScaleGenerator（类）
