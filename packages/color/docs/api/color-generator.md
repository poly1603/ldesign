# 颜色生成（Color Generator）

```ts
import {
  COLOR_GENERATION_PRESETS,
  createColorGenerator,
  generateColorConfig,
} from '@ldesign/color'
```

- generateColorConfig(primary: string)
- createColorGenerator(presetOrConfig)
- ColorGeneratorImpl（类）

