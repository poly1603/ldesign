export default {
  // 入口文件 - 自动检测
  // input: 'src/index.ts', // 可省略，自动检测

  // 输出格式 - 默认 esm 和 cjs
  // formats: ['esm', 'cjs'], // 可省略，使用默认值

  // 生成类型声明文件 - 默认开启
  // dts: true, // 可省略，默认开启

  // 外部依赖 - 自动检测
  // external: [...], // 可省略，自动从 package.json 和依赖分析中获取

  // UMD 全局变量名
  name: 'LDesignTemplate',

  // 全局变量映射 - 自动生成
  // globals: {...}, // 可省略，自动生成
})
